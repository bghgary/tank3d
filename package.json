{
  "name": "tank3d",
  "version": "0.1.0",
  "description": "",
  "private": "true",
  "scripts": {
    "install": "npm-recursive-install --skip-root",
    "dev": "concurrently \"npm --prefix ./app run dev\" \"npm --prefix ./deploy run dev\"",
    "build": "npm --prefix ./app run build && npm --prefix ./deploy run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "recursive-install": "^1.4.0"
  }
}
